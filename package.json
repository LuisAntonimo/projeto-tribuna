{
  "name": "projeto-tribuna",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "check-types": "turbo run check-types"
  },
  "description": "Uma plataforma para escrita e compartilhamento de ensaios, com o foco em debates em formato de blogging",
  "devDependencies": {
    "turbo": "^2.5.5",
    "typescript": "5.8.3",
    "vite": "^7.0.4",
    "eslint": "^9.33.0",
    "prettier": "^3.6.2"
  },
  "engines": {
    "node": ">=18"
  },
  "packageManager": "npm@10.9.2",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "dependencies": {
    "-": "^0.0.1",
    "workspaces": "^0.1.0"
  }
}
